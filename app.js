// Whenever you use Greetr, you just have to say G$ then pass it three parameters (firstName, lastName, language). And what it gives you back is an Object.

var g = G$('John', 'Doe');
console.log(g);
